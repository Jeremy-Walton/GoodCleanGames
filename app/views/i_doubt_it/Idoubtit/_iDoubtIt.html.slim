div ng-app=''
  div.Idoubtit ng-controller="gameView"

    section class="Head"
      ul
        li.Links ng-click="newGame()" New Game
        li.Links ng-click="saveGame()" Save Game
        li.Links ng-click="loadGame()" Load Game

    section.Subheader
      p Welcome to I Doubt It. Lie at your own risk!
      h1
        | {{playerNames()}}
      h1
        | {{playerTurn()}}'s turn
      h4.Rank
          | Current rank: {{currentRank()}}

    section.Content

      section#Hand
        h3 Your Cards
        ul#cards
          li ng-repeat="card in unselectedCards(cards) track by $index"
            div ng-click="cardSelect(card)" class="Highlight" height= "70px" class="{{card.class()}}"

      section#Books
        h3 The Discard Pile
        h5 Pile size: {{pileSize()}}
        ul
          li
            img ng-src="{{discardPile()}}"

      section#Interface
        h3 Selected Cards
        ul#selected
          li ng-repeat="card in selectedCards(cards) track by $index"
            div class="{{card.class()}}"
        span class="Hover" ng-click="play()" Play Cards
        span class="Hover" ng-click="cancel()" Cancel Selection
        h3 Last turn
        span class="Hover" ng-click="doubt()" I Doubt It
        p
          | {{results()}}

      section#Opponent_Info
        h3 Opponent Info
        ul
          li ng-repeat="player in info()"
            | {{player.playerName()}} has {{player.handSize()}} cards.

    section.Foot
      p Game by Jeremy Walton. Made 2/12/2014
